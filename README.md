```bash
npm test
npm run build
```

D2 code blocks on GitHub are rendered by `src/d2/d2-serve` (symlinked from the
devenv bin repo), which shells out to the `d2` CLI. It has to be running for
diagrams to appear.
