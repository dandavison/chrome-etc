{
  "name": "chrome-etc",
  "version": "0.2.0",
  "description": "Chrome extension for enhanced GitHub experience",
  "scripts": {
    "clean": "rm -rf dist dist-test",
    "build": "npm run clean && tsc && npm run copy-static",
    "copy-static": "cp public/manifest.json public/options.html public/icon-*.png dist/",
    "watch": "tsc --watch",
    "test": "npm run test:visual && npm run test:d2",
    "test:visual": "npm run build && npx ts-node --project tsconfig.test.json test/test-comment-fold-visual.ts",
    "test:d2": "npm run build && npx ts-node --project tsconfig.test.json test/test-d2-visual.ts",
    "package": "npm run build && cd dist && zip -r ../extension.zip *"
  },
  "dependencies": {
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/chrome": "^0.0.309",
    "@types/node": "^20.0.0",
    "playwright": "^1.57.0",
    "ts-node": "^10.9.2"
  }
}
