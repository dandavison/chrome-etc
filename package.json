{
  "scripts": {
    "build": "tsc",
    "test": "npm run test:fullwidth && npm run test:mermaid",
    "test:fullwidth": "npm run build && npx ts-node --project tsconfig.test.json test-fullwidth.ts",
    "test:mermaid": "npm run build && npx ts-node --project tsconfig.test.json test-mermaid.ts"
  },
  "dependencies": {
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/chrome": "^0.0.309",
    "@types/node": "^20.0.0",
    "playwright": "^1.57.0",
    "ts-node": "^10.9.2"
  }
}
